{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "client/build"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "installCommand": "cd buildx/client 2>/dev/null || cd client && npm install",
  "buildCommand": "cd buildx/client 2>/dev/null || cd client && npm run build"
}
